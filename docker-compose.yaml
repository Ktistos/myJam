version: '3.8'

services:
  frontend:
    build:
      context: ./front-end
      dockerfile: Dockerfile
    container_name: myjam-frontend-service
    ports:
      - "8080:80" # Expose the Nginx server on host port 8080
    restart: unless-stopped